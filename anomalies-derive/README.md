# anomalies-derive

Procedural macro support for the [`anomalies`](../README.md) crate.

This crate is not intended to be used directly. Add `anomalies` as a dependency and use `#[derive(Anomaly)]` — it re-exports everything from here.
